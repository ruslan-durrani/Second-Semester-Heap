package ruslan_7kd;
import  java.util.*;
import java.util.Arrays;
public class Sessional_Two_Question {
    public static void main(String[] args) {
        if (args.length == 0)
            System.exit(0);
        for (int i = 0; i < args.length; i++)
            System.out.println(args[i]+"--->"+args[i].length());
    }
}
